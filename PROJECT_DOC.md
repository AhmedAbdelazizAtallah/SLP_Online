# 📘 التوثيق الشامل — Sign Language Platform (SLP_Online)

> منصة ويب مترجمة فورية: **لغة إشارة ↔ نص ↔ صوت** — للتواصل بين الأصم والسامعين، مع مكالمات مباشرة وأفاتار ثلاثي الأبعاد ووضع تعليمي تفاعلي.
> اللغتان المدعومتان: **العربية (ArSL — 33 كلاس)** و**الإنجليزية (ASL — 28 كلاس: A-Z + DEL + SPACE)**.

---

## 1) فلسفة التصميم المعماري

المشروع مبني على قرار هندسي أساسي: **أقل عدد ملفات ممكن بدون build pipeline تقليدي (npm/webpack)**:

```
┌─────────────────────────────────────────────────────┐
│  app.html — الواجهة بالكامل (React 18 + Three.js)   │
│  مصدر JSX داخل <script id="app-source">             │
│  الحزمة المجمعة app.compiled.js (مولدة بـ build.ps1)│
└──────────────────┬──────────────────────────────────┘
                   │ HTTP + WebSocket + WebRTC
┌──────────────────▼──────────────────────────────────┐
│  Server.py — FastAPI                                │
│  استدلال TFLite + WebRTC signaling + ملفات ثابتة    │
└──────────────────┬──────────────────────────────────┘
                   │
    arsl_model.tflite (3.7MB — 33 كلاس عربي)
    asl_model.tflite  (3.7MB — 28 كلاس إنجليزي)
```

**أسباب هذا التصميم:**
- **النشر المجاني**: Render free tier بذاكرة 512MB — الموديلات تستخدم **Flex ops** (BiLSTM) لذا لا يعملها `tflite-runtime` الصغير؛ الحل `tensorflow-cpu` كامل.
- **Same-origin API**: الصفحة تنادي الباك إند على نفس الدومين → تعمل على أي نطاق بدون إعدادات.
- **الاستدلال على السيرفر وليس المتصفح** → يعمل حتى على هواتف ضعيفة، والموديلات تبقى خاصة.
- **الحزمة المجمعة مسبقًا** (`app.compiled.js`) بدل Babel في المتصفح (~3MB أقل تحميلًا وأسرع فتحًا)، مع Babel كخطة احتياطية للتطوير فقط.

---

## 2) مسار الذكاء الاصطناعي (AI Pipeline)

```
الكاميرا (640×480)
   │  MediaPipe Hands (modelComplexity:0 — ~45ms/فريم)
   ▼
21 landmark × (x,y,z) = 63 قيمة
   │  انعكاس x لليد اليسرى لتوحيد اليدين
   ▼
Buffer منزلق = 23 فريم (يبدأ الاستدلال عند 8 فريمات MIN_FILL)
   │  POST /predict {sequence, language}
   ▼
TFLite interpreter (مع inference_lock ضد التزامن)
   ▼
softmax probs → argmax → حرف + ثقة + top-3
   │  فلتر الثبات (فرونت إند):
   │  نفس الحرف × stab فريمات متتالية وبثقة ≥ confGate
   │  + بوابة الالتقاط المفرد: الحرف الأخير مُقفل حتى تترك اليد الإشارة
   ▼
Commit الحرف إلى الجملة (مرة واحدة لكل إشارة)
```

### طبقات الموثوقية فوق الموديل
| الطبقة | الوظيفة |
|---|---|
| `SPEED_PRESETS` | smooth / fast / turbo — ضبط حساسية الالتقاط |
| `confGate` | بوابة ثقة 50% / 60% / 80% — ما دونها لا يُكتب |
| بوابة الالتقاط المفرد (`newStab`/`latch`/`addRelease`) | كل حرف يُلتقط **مرة واحدة فقط**: الحرف المُلتقَط يبقى مُقفلًا حتى تترك اليد الإشارة فعليًا (حركة اليد، أو انهيار الثقة، أو خروج اليد من الكادر). تذبذب الـ argmax أثناء الانتقال بين حرفين ليس دليل تحرير، فلا يمكن أن يُكرِّر الحرف السابق. لتكرار نفس الحرف (LL في HELLO) أعِد أداء الإشارة |
| تفريغ النافذة عند حدود الإشارة (`SIGN_BOUNDARY_FRAMES`) | النافذة 23 فريم منزلقة = ~ثانية كاملة عند 22 fps، فكانت كل تنبؤات ما بعد تغيير الحرف تُحسب على نافذة تخلط الحرف القديم بالجديد — وهذا سبب التأخير **وسبب اختلاط ج/ح/خ**. عند رصد انتقال حقيقي تُفرَّغ النافذة ولا تُغذّى بفريمات الحركة، فيصل الجواب النظيف بعد `MIN_FILL` فريم بدل `WINDOW` |
| `modelComplexity: 1` + تخفيض بوابات التتبع إلى 0.5 | الموديل الدقيق للـ landmarks بدل الـ lite: الأصابع المنثنية والمتراكبة (ج/ح/خ، د/ذ) لا تُحسم بالموديل الخفيف. مع تخفيض تلقائي لمرة واحدة لو ثبت الـ FPS تحت `LOW_FPS_LIMIT` |
| `jhkArbitrate` | فاصل هندسي لثلاثية ج/ح/خ: خ = ح + إبهام ممدود للأعلى، ج = نفس الأصابع منحنية للأسفل. نسب مستقلة عن المسافة وعن انعكاس اليد، ولا يعمل إلا لو كان الموديل مُتحيِّرًا فعلًا (شريك في top-3) |
| `CONFUSABLE_AR` | ثلاثية ج/ح/خ متبادلة كاملة (خ كانت غائبة تمامًا)، وأزواج ع/غ و ض/ص و ال/لا، وكل الحدود صارت متبادلة الاتجاه |
| ذاكرة اتجاه اليد (`flipWinRef`) | إعادة المحاولة معكوسة كانت تُضاعف كل رحلة للسيرفر عند كل حرف صعب؛ الآن يُجرَّب الاتجاه الرابح أولًا فتصبح الحالة المستقرة رحلة واحدة |
| استجابة `503` أثناء التحميل | تمنع التنبؤات الوهمية وتفعّل الـ heuristic fallback حتى يصبح الموديل جاهزًا |
| Heuristic fallback | لو السيرفر سقط: قواعد هندسية على landmarks (قبضة=A، كف=B...) تُبقي الترجمة شغالة أوفلاين |

---

## 3) مكونات المشروع ملفًا بملف

### 🐍 Server.py
| Endpoint | النوع | الوظيفة |
|---|---|---|
| `/` | GET/HEAD | تقديم صفحة app.html |
| `/health` | GET | حالة المحرك والموديلات (health check النشر) |
| `/predict` | POST | الاستدلال — محمي: حد 120 فريم، رفض الحمولات الضخمة |
| `/models` | GET | أشكال input/output للموديلين |
| `/api/ice-servers` | GET | STUN دائمًا + TURN من env vars فقط (لا أسرار في الكود) |
| `/ws` | WebSocket | signaling الغرف — حد 8 مشاركين، رسائل ≤300KB، رفض بـ `room-full` |
| `/app.compiled.js` | GET | الحزمة المجمّعة |
| `/favicon.ico` | GET | fallback |

تفاصيل مهمة:
- تحميل الموديلات في thread خلفي (`lifespan`) لفتح المنفذ فورًا ونجاح health checks أثناء warm-up.
- CORS بدون `allow_credentials` (تركيبة صحيحة مع wildcard).
- تنظيف تلقائي لغرف ROOMS عند خروج آخر عضو.

### 🌐 app.html
- Design system CSS كامل بمتغيرات (`--blue`, `--panel`, ...) وخطوط Inter + JetBrains Mono عبر `<link>` مع preconnect.
- React بدون npm: مصدر JSX داخل `<script type="text/plain" id="app-source">`.
- RTL تلقائي عند العربية · aria-live للـ captions والتنبؤات والشات · شاشة خطأ أنيقة (`renderFatal`) بدل أي شاشة سوداء صامتة.
- تدهور آمن بدون WebGL: رسالة بديلة داخل صندوق الأفاتار دون إسقاط التطبيق.

### 🤖 الموديلات
CNN+BiLSTM quantized TFLite — مدخل `[1,23,63]` float32، مدرّبة على datasets عامة (ArSL 33 حرفًا / ASL A-Z).

### ⚙️ ملفات النشر
| ملف | الدور |
|---|---|
| `render.yaml` | Blueprint لـ Render: python 3.11.9، فرانكفورت، threads=1، متغيرات TURN بنمط sync:false |
| `Dockerfile` | صورة python:3.11-slim تشمل كل الملفات بما فيها الحزمة المجمعة |
| `Procfile` | تشغيل Heroku-style بـ `$PORT` |
| `build.ps1` | إعادة توليد `app.compiled.js` من مصدر JSX (esbuild) |

> **قاعدة ذهبية:** بعد أي تعديل على كود React داخل app.html نفّذ:
> ```
> powershell -ExecutionPolicy Bypass -File build.ps1
> ```

---

## 4) الواجهة — التابات الستة

### 🖐 SIGN — الترجمة الحية
- فيد كاميرا مع رسم landmarks ومرايا · عداد FPS/BUFFER · PRED badge بالثقة.
- Top-3 predictions بأشرطة نسب.
- Sentence builder: Space/Backspace من الكيبورد، SAVE/COPY/SHARE/SPEAK، مسح آمن يحفظ قبل المسح.
- اقتراحات كلمات (قاموس AR 15 / EN 14 كلمة) بتطابق الكلمة الأخيرة.
- عرض تفاصيل الموديل (dataset/features/timesteps/classes).

### 🏠 SIGN ROOMS — الغرف المباشرة
- غرف بكود 5 حروف أو رابط دعوة `?room=CODE` · توليد كود عشوائي بدون أحرف ملتبسة · Enter في خانة الكود أو PIN ينضم مباشرة.
- اختيار دور: ✋ أصم / 🗣 سامع + اسم معروض محفوظ. الدور يستنبط AUTO-SPEAK **فقط** إن لم يكن المستخدم قد اختاره صراحةً (الزرّان يكتبان نفس المفتاح، وكان الدور يمحو الاختيار بصمت).
- **فيديو WebRTC mesh** (P2P) مع TURN relay للشبكات الصارمة.
- **Fallback تلقائي**: بث JPEG عبر WebSocket فقط عندما لا يوجد فيديو WebRTC (يوفر Bandwidth)، والعرض يفضّل WebRTC دائمًا.
- DataChannel + WebSocket برسائل caption موحّدة تحمل `mid` — dedupe يمنع ظهور الرسالة/نطقها مرتين، والمسارَان يتفقان على نفس `kind` الافتراضي.
- **`kind` يحمل مصدر النص لا الزر**: ما يُبنى بالإشارة يُرسل `kind:'sign'` مهما كان زر الإرسال. قبل ذلك كان زر الإرسال الأساسي يُثبّت `'chat'`، فكان كل ما يشكّله الأصم لا يُنطق عند السامع ولا يظهر كـ Caption كبير — أي أن الميزتين الموجودتين لهذه الحالة تحديدًا كانتا معطّلتين.
- **AUTO-SPEAK** = «اقرأ كل رسالة واردة من الطرف الآخر بصوت مسموع» (`shouldSpeakIncoming`): لم يعد مشروطًا بـ `kind`، و SOUND OFF يتجاوزه دائمًا.
- **أفاتار داخل المكالمة** (`av-host-net`): تشغيل الإشارة يظهر في لوحة صغيرة فوق الـ stage عند الطرفين. كان الـ host غير موجود، فكان كل تشغيل يذهب إلى الـ keeper المخفي (`left:-9999`) ولا يراه أحد.
- شات ملون حسب الدور (أصم/سامع/نظام) · Caption كبير overlay مع aria-live · نطق TTS عربي/إنجليزي.
- أدوات التحكم: مايك (مع renegotiation حية عند التفعيل وسط الاتصال) / كاميرا / صوت / عرض split-PIP / ملء الشاشة / تسجيل. زر SPEAK واحد فقط: بطاقة الشات على الديسكتوب، وورقة ⋯ على الموبايل — وكل أزرار ورقة ⋯ تُغلقها لأنها تحجب الفيديو.
- Speech-to-text يذهب لمسودة **تتم مراجعتها قبل الإرسال** — لا إرسال تلقائي أبدًا. أزرار الإرسال معطّلة قبل الانضمام (لكن تجهيز المسودة مسموح).
- **التسجيل المحلي** يُحفظ من `onstop` (لا يفقد آخر مقطع)، و LEAVE يحفظ التسجيل الجاري بدل إتلافه، و REC يرفض كاميرا مغلقة بدل تسجيل فيديو أسود.
- **موثوقية التعرف الصوتي**: جيل (generation token) يضمن وجود مُتعرّف صوتي واحد فقط — أي مؤقّت إعادة تشغيل قديم يُلغى فلا تتكرر الكلمات، والنتائج النهائية تُخزَّن فور وصولها فلا تضيع كلمات عند إعادة التشغيل التلقائي (انقطاع شبكة/صمت). ومسح الخانة يمحو النص المُخزَّن أيضًا فلا يُعاد إلحاقه.

### 🧏 TEXT TO SIGN
- Tokenizer ذكي: `ل+ا`→لا، `ا+ل`→ال، إزالة التشكيل، الرموز غير المدعومة tokens نصية TXT.
- تسلسل بطاقات tokens بتقدم أخضر ونسبة إنجاز + شرح كل إشارة.
- الأفاتار ينفذ الجملة كاملة (سرعات 400/650/900ms + AUTO بعد الإدخال الصوتي).
- تكامل: إشارتك الصحيحة أمام الكاميرا تقدّم التسلسل تلقائيًا.

### 🎓 LEARN & QUIZ
- Timed Quiz (5/8/12/ALL × 8/10/15 ثانية) + Free practice.
- معاينة كاميرا حية · Scoreboard لكل حرف · نتائج نهائية بالنسب ومتوسط زمن الاستجابة.

### 📊 HISTORY • STATS (تاب مدمج)
- جمل محفوظة في localStorage: نسخ / نطق / تحويل لإشارات / حذف / تصدير .TXT.
- إحصائيات الجلسة والعمر: عدد الإشارات، السرعة (commit/min)، متوسط الثقة، أكثر الحروف استخدامًا (رسوم أعمدة).

### ⚙️ SETTINGS
سرعة الالتقاط · بوابة الثقة · عنوان السيرفر + CHECK · إعادة تحميل معلومات الموديل · **Developer diagnostics** مطوية (pipeline/buffer/raw features) — أدوات المطورين غير مرئية للمستخدم العادي.

---

## 5) الأفاتار ثلاثي الأبعاد

- يد مبنية برمجيًا: راحة + كعب + ثمار إبهام + 5 سلاسل مفاصل، مواد MeshPhysicalMaterial بجلد procedural (bump + sheen) وأظافر.
- **Spring-damper dynamics** لكل إصبع (stiffness مختلفة 58–96، ζ=0.82) لحركة عضلية طبيعية.
- **33 pose عربي + 28 إنجليزي** مع حركات مركبة: لا (L→ألف)، رسم J و Z، مسحة DEL، نقاط الحروف العربية (ت/ث/ش/ض...) بحركة الإبهام.
- Idle micro-motion (تنفس وانحرافات) · ظلال PCF soft · ACES tone mapping.
- **Instance واحدة دائمة**: portal بين تابي CALL/BUILD مع keeper مخفي — لا إعادة بناء مكثفة ولا تسريب WebGL contexts.

---

## 6) الأمان والحمايات المطبقة

✅ STUN مدمج افتراضيًا، وTURN يُقرأ من env vars فقط حتى لا تُخزّن بيانات الدخول في الكود
✅ حد حجم /predict (120 فريم + سقف بايتات) ضد DoS
✅ سعة الغرف 8 مشاركين (MAX_ROOM_SIZE) + رسائل WS ≤300KB + تنقية المعرفات
✅ رسالة رفض `room-full` للعميل
✅ CORS صحيح (بدون credentials مع wildcard)
✅ dedupe الرسائل (`mid`) ضد التكرار والتشويش الصوتي
✅ شاشة خطأ مقروءة بدل أي انهيار صامت
⚠️ **تنبيه:** بيانات TURN القديمة التي ظهرت سابقًا في تاريخ Git اعتبرها مسرّبة — قم بـ rotate من حساب metered.ca.

---

## 7) الجودة والاختبارات

```
py_compile Server.py            ✓     node --check app.compiled.js      ✓
esbuild transform (بناء)        ✓     اختبار UI حي jsdom (mount فعلي)   ✓
/health /models /ice-servers    ✓     /predict رفض oversized بـ 400     ✓
استدلال حقيقي (99.6% ثقة)       ✓     WebSocket room-full + broadcast   ✓
smoke test: 162 عنصر، 0 أخطاء   ✓     WebGL-missing graceful degrade    ✓
```

---

## 8) التشغيل والنشر

### محليًا (Windows)
```
cd D:\SLP_Online
set PORT=8000
python Server.py          :: بيئة فيها TensorFlow
:: أو بيئة المشروع الأصلي:
D:\SLP\venv\Scripts\python.exe Server.py
```

### النشر على Render (مجاني)
1. ارفع المجلد على GitHub.
2. render.com → New + → Blueprint → اختر المستودع → Apply.
3. من Environment أكمل القيم المطلوبة: `TURN_URL`, `TURN_USERNAME`, `TURN_CREDENTIAL` (حساب مجاني على metered.ca) واختياريًا `MAX_ROOM_SIZE`.
4. الرابط: `https://<app>.onrender.com` — HTTPS يجعل الكاميرا والمايك يعملان.

> السيرفر ينام بعد 15 دقيقة بلا زوار — cron-job.org يزور `/health` كل 10 دقائق يُبقيه مستيقظًا.

### خطوات ما بعد أي تعديل
```
عدّلت كود React في app.html؟ → powershell -ExecutionPolicy Bypass -File build.ps1
ثم Commit & Push → Render يعيد النشر تلقائيًا.
```

---

## 9) حدود النظام الحالية (بشفافية)

1. ترجمة **حرف بحرف** — التطور الطبيعي التالي: word-level model أو CTC decoding.
2. **يد واحدة** فقط — الإشارات ثنائية اليد غير مدعومة.
3. **Mesh بدون SFU** — مكالمات 4+ تستهلك upstream كبير لكل مشارك.
4. الغرف **بدون مصادقة** — من يعرف الكود يدخل.
5. اعتماد الواجهة على CDN خارجي (unpkg) — يمكن لاحقًا self-host للمكتبات.
6. لا توجد اختبارات آلية مخزنة بالمستودع (اختبارات jsdom/WS أُجريت يدويًا أثناء التطوير).

---

*آخر تحديث لهذا التوثيق: أغسطس 2026 — يغطي نسخة ما بعد جلسات الإصلاح وإعادة تصميم الواجهة.*

---

# 🆕 SLP_Next — الميزات الإضافية (A–M)

> هذه النسخة نسخة مستقلة مطوّرة عن SLP_Online بنفس البنية + 12 ميزة جديدة. كلها قابلة للإيقاف/التشغيل من Settings حيث يلزم.

## الترجمة الذكية
| الميزة | الوصف |
|---|---|
| **AUTO-SPACE** | إيدك تختفي أو تهد ثانية؟ مسافة تلقائية في الجملة — بيكتب كلام بسلاسة |
| **AUTO-WORD** | آخر كلمة طابقت كلمة من القاموس → مسافة تلقائية (ON/OFF) |
| **SMOOTHING** | تنعيم EMA لثقة كل حرف قبل الالتقاط = ارتجاف أقل |

## الغرف (SIGN ROOMS)
| الميزة | الوصف |
|---|---|
| **Reconnect تلقائي** | أي انقطاع WS = محاولات backoff حتى 10s + heartbeat كل 15s يكشف الاتصال الميت (45s watchdog) |
| **PIN للغرفة** | أول منشئ يحط PIN يقفل الغرفة عليه (sha256 على السيرفر)، الدعوة تتضمن الـ PIN |
| **REC محلي** | تسجيل كاميرتك (+مايك لو شغال) webm وتحميله فورًا |
| **أسماء وألوان** | اسم كل مشارك فوق مربعه بلون ثابت مشتق من الـ id + الدور (DEAF/HEARING) |

## التعليم
| الميزة | الوصف |
|---|---|
| **تاب ALPHABET** | معرض كل الإشارات (31 عربي / 26 إنجليزي) بالشرح — اضغط الكارت والأفاتار يمثّلها فورًا |
| **MY WEAK quiz** | وضع مصدر جديد في الكويز: يبني تدريبًا من أسوأ حروفك وفق إحصائيات الثقة المحفوظة |

## البنية والبنية التحتية
| الميزة | الوصف |
|---|---|
| **Self-hosted libs** | React/ReactDOM/Three من `vendor/` عبر السيرفر نفسه — لا اعتماد على unpkg في الإنتاج (يبقى فقط لـ Babel الاحتياطي) |
| **PWA** | manifest + أيقونات + service worker: تثبيت كتطبيق، وأوفلاين جزئي (TEXT TO SIGN / ALPHABET / QUIZ / HISTORY تعمل بدون سيرفر) |
| **Error reporting** | أي خطأ غير ملتقط في المتصفح يُرسل `/api/client-error` ويُسجّل في client_errors.log (محدود 20/دقيقة/IP) |

## الاختبارات والتحقق الآلي
```
tests/run.ps1        ← الحزمة الكاملة: build + syntax + live endpoints + UI smoke + WS
tests/ui.smoke.test.js  jsdom: mount فعلي، تابات، معرض، portal الأفاتار
tests/ws.test.py     roster · PIN قبول/رفض · ping→pong · hello relay · room-full
.github/workflows/ci.yml   يعمل كل ما سبق تلقائيًا على كل push
```

## نقاط تشغيلية
- متغيرات بيئة جديدة اختيارية: `MAX_ROOM_SIZE` (افتراضي 8)، `ERR_RATE_LIMIT` (افتراضي 20).
- Dockerfile يشمل vendor/ وicons/ وmanifest.json وsw.js ضمن السياق.
- تمت إزالة ميزتي **VIDEO FILE TRANSLATION** (رفع فيديو من تاب SIGN) و**VIDEO CALL** — الغرف المباشرة (SIGN ROOMS) تغني عنهما.
